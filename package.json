{
  "name": "clodriver",
  "version": "0.1.0",
  "description": "AI-powered MUD server with ColdMUD-inspired design",
  "main": "lib/server.coffee",
  "type": "commonjs",
  "scripts": {
    "test": "node --enable-source-maps -r ./test-setup.js --test 't/**/*.coffee'",
    "test:watch": "node --enable-source-maps -r ./test-setup.js --test --watch 't/**/*.coffee'"
  },
  "keywords": [
    "mud",
    "llm",
    "ai",
    "coldmud"
  ],
  "author": "Robert de Forest <robert@defore.st>",
  "license": "MIT",
  "dependencies": {
    "coffeescript": "^2.7.0"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "source-map-support": "^0.5.21"
  }
}
